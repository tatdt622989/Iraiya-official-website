---
import LabCard from '../../components/LabCard.astro';
import { labs } from '../../data/labs';
import { getDictionary } from '../../i18n';
import { isLocale, LOCALES, type Locale } from '../../i18n/config';
import BaseLayout from '../../layouts/BaseLayout.astro';

export function getStaticPaths() {
  return LOCALES.map((locale) => ({ params: { locale } }));
}

const localeParam = Astro.params.locale;
if (!isLocale(localeParam)) {
  throw new Error(`Unsupported locale: ${localeParam}`);
}
const locale: Locale = localeParam;
const t = getDictionary(locale);
---

<BaseLayout locale={locale} path="/labs" title={t.pages.labs.metaTitle} description={t.pages.labs.metaDescription}>
  <section class="page-hero">
    <div class="site-shell">
      <h1>{t.pages.labs.title}</h1>
      <p>{t.pages.labs.description}</p>
    </div>
  </section>
  <section class="section-tight">
    <div class="site-shell">
      <div class="grid grid-4">
        {labs.map((lab) => <LabCard lab={lab} locale={locale} />)}
      </div>
    </div>
  </section>
</BaseLayout>

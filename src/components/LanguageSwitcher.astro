---
import { localeLabels, LOCALES, routeForLocale, type Locale } from '../i18n/config';

interface Props {
  locale: Locale;
  path: string;
}

const { locale, path } = Astro.props;
---

<div class="language-switcher" aria-label="Language">
  {LOCALES.map((availableLocale) => (
    <a
      class:list={['language-link', availableLocale === locale && 'is-active']}
      href={routeForLocale(availableLocale, path)}
      aria-current={availableLocale === locale ? 'page' : undefined}
    >
      {localeLabels[availableLocale]}
    </a>
  ))}
</div>

<style>
  .language-switcher {
    display: flex;
    align-items: center;
    gap: 6px;
    flex-wrap: wrap;
    justify-content: flex-end;
  }

  .language-link {
    display: inline-flex;
    min-width: 38px;
    min-height: 34px;
    align-items: center;
    justify-content: center;
    border: 1px solid rgba(148, 163, 184, 0.16);
    border-radius: 8px;
    padding: 6px 9px;
    color: #cbd5e1;
    font-size: 0.78rem;
    font-weight: 800;
  }

  .language-link:hover,
  .language-link.is-active {
    border-color: rgba(34, 211, 238, 0.42);
    color: #f8fafc;
    background: rgba(34, 211, 238, 0.1);
  }

  @media (max-width: 420px) {
    .language-switcher {
      max-width: 164px;
      gap: 4px;
    }

    .language-link {
      min-width: 34px;
      min-height: 32px;
      padding: 5px 7px;
      font-size: 0.72rem;
    }
  }
</style>
